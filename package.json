{
  "name": "onlineshop_training-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon server.js",
   "dev": "nodemon server.js",
    "dev-windows": "nodemon server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "express-session": "^1.17.2",
    "mongoose": "^6.2.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "nodemonConfig": {
    "ignore": [
      "client/*"
    ]
  }
}
